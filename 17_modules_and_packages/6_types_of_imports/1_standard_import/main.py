# main.py

# Import the entire example_module so that we can access its functions,
# classes, and variables.
import example_module

# We use the module name 'example_module' as a prefix to call the greet
# function.
print(example_module.greet())
