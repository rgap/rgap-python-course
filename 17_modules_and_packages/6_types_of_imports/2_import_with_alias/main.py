# main.py

# Import the entire example_module, but with an alias 'em' for easier
# reference.
import example_module as em

# Now we can use 'em' as the module name to call the greet function.
print(em.greet())
