# main.py

# Import only the greet function from the example_module, not the entire
# module.
from example_module import greet

# We can now call greet directly without using the module name as a prefix.
print(greet())
