# main.py

# Import all functions, classes, and variables from example_module into
# the current namespace.
from example_module import *

# We can now call greet and farewell directly without using the module
# name as a prefix.
print(greet())
print(farewell())
