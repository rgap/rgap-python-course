# Iterating Over a Set

fruits_set = {"apple", "banana", "cherry"}

# Iterating using a for loop
for fruit in fruits_set:
    print(f"Fruit: {fruit}")  # Outputs each fruit in the set

# Note: The order of elements when iterating over a set is not guaranteed.
